console.log("Oi");

// Constantes

const name = "Alexandre";

// Let -  let it change

let age = 26;

let isCute = true;

// Tipos de dados no JS

// Strings

const str = "String com aspas duplas";
const str2 = "Aspas simples, CUIDADO";
const str3 = `Meu nome é ${name}`;
const str4 = "12";

console.log(str3);

console.log(str2.length);
console.log(str.toUpperCase());

// Numbers

const num = 50;

console.log(num * 2);

console.log(typeof (num + str4));

console.log(2 + true);

// Booleans

const isLovely = false;

// Operadores condicionais

// ==
// ===
// !

// !=

// !==

// >
// >=

// <
// <=

// Operadores para mais de uma condição

// condição || condição
// condição && condição

if (condição) {
  // Se a condição for TRUE, a gente executa o que ta aqui
}

if (condição) {
} else {
  // Se a condição for FALSE, a gente executa o que está aqui
}

if (condição) {
} else if (condição) {
} else {
}

// Exemplo

if (age < 18) {
  console.log("Você é menor de idade");
}

// Obs: A gente pode guardar FUNÇÕES em variáveis

// SOON

// Arrays
// Objetos
